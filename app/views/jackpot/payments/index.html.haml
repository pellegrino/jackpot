%h1 Payments 

- if @payments.present? 
  %table#latest-payments.table.table-striped
    %thead
      %tr
        %th Customer Name
        %th Amount
        %th Date Created 
    %tbody
      - @payments.each do |payment|
        %tr 
          %td.customer-name= payment.customer_email
          %td.amount=        number_to_currency(payment.amount)
          %td.date_created=  distance_of_time_in_words_to_now(payment.created_at)

